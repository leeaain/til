# String
> ## 메소드
```js
str.length

str[n]

str.slice(0, 3) 
str.substring(0, 3)  // 위와 동일

str.toUpperCase()
str.toLowerCase()

str.indexOf('a')
str.lastIndexOf('a')

str.includes('a')

str.split(" ") // --> " "를 기준으로 arr로 변환

str.split(" ").join("") // 다시 문자열로
```
